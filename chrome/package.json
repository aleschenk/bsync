{
  "name": "bsync-chrome-extension",
  "version": "1.0.0",
  "description": "Chrome extension for syncing browser tabs",
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "clean": "rm -rf dist/*",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/chrome": "^0.0.254"
  },
  "keywords": ["chrome-extension", "typescript", "tabs-sync"],
  "author": "BSync Team",
  "license": "MIT"
}
